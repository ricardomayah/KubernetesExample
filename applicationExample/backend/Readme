1. se debe de crear la imagen de docker con el comando
    docker build -t k8s-hands-on-example:v2 -f .\Dockerfile .

2. Crear los recuros en kubernetes con el comando
    kubectl apply -f backend.yaml